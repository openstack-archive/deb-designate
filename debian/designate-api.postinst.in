#!/bin/sh

set -e

#PKGOS-INCLUDE#

if [ "$1" = "configure" ]  || [ "$1" = "reconfigure" ] ; then
	. /usr/share/debconf/confmodule
	pkgos_register_endpoint_postinst designate designate dns "Designate DNS Service" 9001 "/v1/"
	db_stop
fi

#DEBHELPER#

exit 0
